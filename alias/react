# react commands
alias addts='touch tsconfig.json && npm install --save-dev typescript @types/react @types/node'
alias jstots='for foo in *.js; do mv $foo `basename $foo .js`.tsx; done'
tailwindnext() { npx create-next-app -e with-tailwindcss "$1" }
